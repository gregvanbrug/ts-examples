import { add, join } from './typing';
import { Colors, ColorsByName, Codes } from './enums';
import { greetPets } from './interfaces';
import { Person } from './class';

export default { add, join, Colors, ColorsByName, Codes, greetPets, Person };
